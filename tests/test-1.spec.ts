// import { test, expect } from '@playwright/test';

// test('test', async ({ page }) => {
//   await page.goto('http://localhost:5173/login');
//   await page.getByRole('tab', { name: 'Cadastre' }).click();
// });
// await page.getByPlaceholder('Usuario').click();
// await page.getByPlaceholder('Usuario').fill('usuario_mapa');
// await page.getByRole('tab', { name: 'Entrar com senha' }).click();
// await page.locator('label').nth(1).click();
// await page.getByPlaceholder('Email').fill('usuario_mapa');
// await page.locator('label').nth(2).click();
// await page.locator('#password').fill('senha_forte@');
// await page.getByRole('button', { name: 'Continuar' }).click();
// await page.getByPlaceholder('Email').click();
// await page.getByPlaceholder('Email').press('ControlOrMeta+a');
// await page.getByPlaceholder('Email').fill('usuario_mapa@hotmail.com');
// await page.getByRole('button', { name: 'Continuar' }).click();
// await page.getByRole('link', { name: 'Começar' }).click();
// await page.getByPlaceholder('Nome do mapa').click();
// await page.getByPlaceholder('Nome do mapa').fill('dasdas');
// await page.locator('div').filter({ hasText: 'Criar um mapa Criar um mapa' }).nth(3).click();